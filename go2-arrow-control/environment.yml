name: demo_workshops
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - Flask==3.0.0
    - opencv-python==4.8.1.78
    - numpy==1.24.3
    - Werkzeug==3.0.1
    - tflite-runtime==2.14.0
    - Pillow==10.1.0
    - pyOpenSSL

# Unitree GO2 SDK: install separately following the project's README
# (e.g. pip install unitree_sdk2py)
